#lang pollen
◊(require racket/string)

index.html
◊`(blog
   ,@(map string->symbol
	  (remove "index.html"
		  (map (lambda (s) (substring s 0 (- (string-length s) 3)))
		       (filter (lambda (p) (string-suffix? p ".html.pm"))
			       (map path->string (directory-list "blog")))))))
